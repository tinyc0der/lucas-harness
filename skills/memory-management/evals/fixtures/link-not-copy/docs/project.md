# Project Contract

## Delivery model

Use trunk-based development: short-lived branches merge into `main`, which must
remain releasable. This keeps integration feedback fast and limits branch drift.
